SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here
RESTAURANT_ID=your-restaurant-uuid-here
# Optional: set a hub label (any text, e.g. counter-pc or kitchen-pc) when you have
# more than one Windows PC. Each device in Settings → Printers must have the matching
# Hub label. This PC will only handle jobs for those devices.
# Leave blank or remove this line if you have a single hub PC.
HUB_ID=
